## Q46: Spring MVC request lifecycle

Walk through what happens from the moment an HTTP request hits a Spring MVC application to the moment a response is sent back. Name each component involved and its responsibility.

Follow-up: what happens differently when the controller returns `@ResponseBody` vs a view name?
