# 56. What is the difference between `@Component` and `@Bean` in Spring?

Explain when to use `@Component` vs `@Bean`, how Spring discovers each, and give real-world examples of when you'd pick one over the other.
