## Q25: What's the difference between `CrudRepository`, `PagingAndSortingRepository`, `JpaRepository`, and `ListCrudRepository` in Spring Data? When would you use each?

Cover: the inheritance chain, what each adds, when to use `JpaRepository` vs the lighter alternatives, flush/saveAll/batch behavior, ListCrudRepository (Spring Data 3+).
