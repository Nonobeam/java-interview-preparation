## Q6: What is the N+1 query problem and how do you solve it?

Explain what causes the N+1 query problem in JPA/Hibernate. Cover all solutions: JOIN FETCH, @EntityGraph, @BatchSize, DTO projection, SUBSELECT. When to use which? How to detect N+1? What is the Cartesian product / MultipleBagFetchException pitfall with multiple JOIN FETCHes?
