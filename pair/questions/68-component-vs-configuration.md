# 73. What is the difference between `@Component` and `@Configuration` in Spring?

Both register a class as a bean — so why does Spring have two annotations? Explain how they differ in **purpose**, in how Spring proxies them, and what actually breaks if you put `@Bean` methods inside a `@Component` instead of `@Configuration`.
