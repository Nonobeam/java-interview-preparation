## Q18: Is OFFSET the page number? How does OFFSET/FETCH pagination work?

OFFSET is rows to skip, not page number. How to calculate OFFSET from page number. Why large OFFSET is slow and keyset pagination as an alternative.
