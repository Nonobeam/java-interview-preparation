## Q24: How do you write a hierarchical query in Oracle using `CONNECT BY`? Give a logistics example such as tracing a container's transshipment route or traversing an organizational hierarchy.

Cover: `CONNECT BY PRIOR`, `START WITH`, `LEVEL` pseudocolumn, `SYS_CONNECT_BY_PATH`, ordering with `ORDER SIBLINGS BY`, cycle detection with `NOCYCLE`, real use case for container routing.
